name: p21_ip_info
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  dart_ping: ^9.0.1
  flutter:
    sdk: flutter
  flutter_map: ^8.2.1
  http: ^1.5.0
  latlong2: ^0.9.1
  shared_preferences: ^2.5.3
  world_flags: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
